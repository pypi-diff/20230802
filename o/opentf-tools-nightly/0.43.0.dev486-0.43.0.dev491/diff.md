# Comparing `tmp/opentf_tools_nightly-0.43.0.dev486-py3-none-any.whl.zip` & `tmp/opentf_tools_nightly-0.43.0.dev491-py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,16 +1,16 @@
-Zip file size: 41639 bytes, number of entries: 14
--rw-r--r--  2.0 unx    25131 b- defN 23-Aug-01 17:11 opentf/tools/ctl.py
--rw-r--r--  2.0 unx    14035 b- defN 23-Aug-01 17:11 opentf/tools/ctlcommons.py
--rw-r--r--  2.0 unx    32425 b- defN 23-Aug-01 17:11 opentf/tools/ctlconfig.py
--rw-r--r--  2.0 unx     8878 b- defN 23-Aug-01 17:11 opentf/tools/ctlnetworking.py
--rw-r--r--  2.0 unx    11908 b- defN 23-Aug-01 17:11 opentf/tools/ctlqualitygate.py
--rw-r--r--  2.0 unx    29105 b- defN 23-Aug-01 17:11 opentf/tools/ctlworkflows.py
--rw-r--r--  2.0 unx     3188 b- defN 23-Aug-01 17:11 opentf/tools/done.py
--rw-r--r--  2.0 unx     9391 b- defN 23-Aug-01 17:11 opentf/tools/ready.py
--rw-rw-rw-  2.0 unx    11357 b- defN 23-Aug-01 17:12 opentf_tools_nightly-0.43.0.dev486.dist-info/LICENSE
--rw-r--r--  2.0 unx     1806 b- defN 23-Aug-01 17:12 opentf_tools_nightly-0.43.0.dev486.dist-info/METADATA
--rw-r--r--  2.0 unx       92 b- defN 23-Aug-01 17:12 opentf_tools_nightly-0.43.0.dev486.dist-info/WHEEL
--rw-r--r--  2.0 unx      139 b- defN 23-Aug-01 17:12 opentf_tools_nightly-0.43.0.dev486.dist-info/entry_points.txt
--rw-r--r--  2.0 unx        7 b- defN 23-Aug-01 17:12 opentf_tools_nightly-0.43.0.dev486.dist-info/top_level.txt
--rw-rw-r--  2.0 unx     1267 b- defN 23-Aug-01 17:12 opentf_tools_nightly-0.43.0.dev486.dist-info/RECORD
-14 files, 148729 bytes uncompressed, 39507 bytes compressed:  73.4%
+Zip file size: 41412 bytes, number of entries: 14
+-rw-r--r--  2.0 unx    24013 b- defN 23-Aug-02 13:26 opentf/tools/ctl.py
+-rw-r--r--  2.0 unx    14327 b- defN 23-Aug-02 13:26 opentf/tools/ctlcommons.py
+-rw-r--r--  2.0 unx    32425 b- defN 23-Aug-02 13:26 opentf/tools/ctlconfig.py
+-rw-r--r--  2.0 unx     8878 b- defN 23-Aug-02 13:26 opentf/tools/ctlnetworking.py
+-rw-r--r--  2.0 unx    11896 b- defN 23-Aug-02 13:26 opentf/tools/ctlqualitygate.py
+-rw-r--r--  2.0 unx    28527 b- defN 23-Aug-02 13:26 opentf/tools/ctlworkflows.py
+-rw-r--r--  2.0 unx     3188 b- defN 23-Aug-02 13:26 opentf/tools/done.py
+-rw-r--r--  2.0 unx     9391 b- defN 23-Aug-02 13:26 opentf/tools/ready.py
+-rw-rw-rw-  2.0 unx    11357 b- defN 23-Aug-02 13:26 opentf_tools_nightly-0.43.0.dev491.dist-info/LICENSE
+-rw-r--r--  2.0 unx     1806 b- defN 23-Aug-02 13:26 opentf_tools_nightly-0.43.0.dev491.dist-info/METADATA
+-rw-r--r--  2.0 unx       92 b- defN 23-Aug-02 13:26 opentf_tools_nightly-0.43.0.dev491.dist-info/WHEEL
+-rw-r--r--  2.0 unx      139 b- defN 23-Aug-02 13:26 opentf_tools_nightly-0.43.0.dev491.dist-info/entry_points.txt
+-rw-r--r--  2.0 unx        7 b- defN 23-Aug-02 13:26 opentf_tools_nightly-0.43.0.dev491.dist-info/top_level.txt
+-rw-rw-r--  2.0 unx     1267 b- defN 23-Aug-02 13:26 opentf_tools_nightly-0.43.0.dev491.dist-info/RECORD
+14 files, 147313 bytes uncompressed, 39280 bytes compressed:  73.3%
```

## zipnote {}

```diff
@@ -18,26 +18,26 @@
 
 Filename: opentf/tools/done.py
 Comment: 
 
 Filename: opentf/tools/ready.py
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev486.dist-info/LICENSE
+Filename: opentf_tools_nightly-0.43.0.dev491.dist-info/LICENSE
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev486.dist-info/METADATA
+Filename: opentf_tools_nightly-0.43.0.dev491.dist-info/METADATA
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev486.dist-info/WHEEL
+Filename: opentf_tools_nightly-0.43.0.dev491.dist-info/WHEEL
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev486.dist-info/entry_points.txt
+Filename: opentf_tools_nightly-0.43.0.dev491.dist-info/entry_points.txt
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev486.dist-info/top_level.txt
+Filename: opentf_tools_nightly-0.43.0.dev491.dist-info/top_level.txt
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev486.dist-info/RECORD
+Filename: opentf_tools_nightly-0.43.0.dev491.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## opentf/tools/ctl.py

```diff
@@ -10,15 +10,15 @@
 # distributed under the License is distributed on an "AS IS" BASIS,
 # WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
 """opentf-ctl"""
 
-from typing import List
+from typing import Any, Dict, List
 
 import json
 import sys
 
 import jwt
 
 from opentf.tools.ctlcommons import (
@@ -277,15 +277,15 @@
   opentf-ctl check token TOKEN using NAME [options]
 
 Use "opentf-ctl options" for a list of global command-line options (applies to all commands).
 '''
 
 
 ########################################################################
-# Eventbus
+# Subscriptions
 
 
 SUBSCRIPTION_COLUMNS = (
     'NAME:.metadata.name',
     'ENDPOINT:.spec.subscriber.endpoint',
     'CREATION:.metadata.creationTimestamp',
     'COUNT:.status.publicationCount',
@@ -298,56 +298,40 @@
     'ENDPOINT:.spec.subscriber.endpoint',
     'CREATION:.metadata.creationTimestamp',
     'COUNT:.status.publicationCount',
     'SUBSCRIPTIONS:.metadata.annotations.*',
 )
 
 
+def _get_subscriptions() -> Dict[str, Any]:
+    return _get(_eventbus(), '/subscriptions', 'Could not get subscriptions list')[
+        'items'
+    ]
+
+
 def list_subscriptions() -> None:
     """List all active subscriptions.
 
-    Outputs information in CSV format (using ',' as a column delimiter).
+    Outputs information in requested format.
 
     # Raised exceptions
 
     Abort with an error code 1 if the orchestrator replied with a non-ok
     code.
 
     Abort with an error code 2 if another error occurred.
     """
-    what = _get(_eventbus(), '/subscriptions', 'Could not get subscriptions list')
-
     generate_output(
-        list(what['items'].values()), SUBSCRIPTION_COLUMNS, WIDE_SUBSCRIPTION_COLUMNS
+        _get_subscriptions().values(), SUBSCRIPTION_COLUMNS, WIDE_SUBSCRIPTION_COLUMNS
     )
 
 
-def _complete_subscriptionid(prefix: str) -> str:
-    response = _get(
-        _eventbus(),
-        '/subscriptions',
-        'Could not get subscriptions list',
-    )
-    ids = [ids for ids in response['items'] if ids.startswith(prefix)]
-    if len(ids) == 1:
-        return ids.pop()
-    if ids:
-        _error(
-            'Ambiguous subscription ID prefix "%s", matches:\n- %s',
-            prefix,
-            '\n- '.join(ids),
-        )
-    else:
-        _error('Subscription ID prefix "%s" matches no known subscription ID.', prefix)
-    sys.exit(2)
-
-
 def delete_subscription(subscription_id: str) -> None:
     """Cancel a subscription."""
-    subscription_id = _ensure_uuid(subscription_id, _complete_subscriptionid)
+    subscription_id = _ensure_uuid(subscription_id, _get_subscriptions)
     what = _delete(
         _eventbus(),
         f'/subscriptions/{subscription_id}',
         'Could not delete subscription',
     )
     print(what['message'])
 
@@ -371,15 +355,15 @@
     'STATUS:.status.phase',
 )
 
 
 def list_channels() -> None:
     """List all active agents.
 
-    Outputs information in CSV format (using ',' as a column delimiter).
+    Outputs information in requested format.
 
     # Raised exceptions
 
     Abort with an error code 1 if the orchestrator replied with a non-ok
     code.
 
     Abort with an error code 2 if another error occurred.
@@ -396,15 +380,15 @@
 
 # Namepaces
 
 
 def list_namespaces() -> None:
     """List namespaces.
 
-    Outputs information in CSV format (using ',' as a column delimiter).
+    Outputs information in requested format.
 
     # Raised exceptions
 
     Abort with an error code 1 if the orchestrator replied with a non-ok
     code.
 
     Abort with an error code 2 if another error occurred.
@@ -458,66 +442,48 @@
     'TAGS:.spec.tags',
     'REGISTRATION_TIMESTAMP:.metadata.creationTimestamp',
     'LAST_SEEN_TIMESTAMP:.status.lastCommunicationTimestamp',
     'RUNNING_JOB:.status.currentJobID',
 )
 
 
-def list_agents() -> None:
-    """List all active agents.
-
-    Outputs information in CSV format (using ',' as a column delimiter).
-
-    # Raised exceptions
-
-    Abort with an error code 1 if the orchestrator replied with a non-ok
-    code.
-
-    Abort with an error code 2 if another error occurred.
-    """
+def _get_agents():
     what = _get(_agentchannel(), '/agents', 'Could not get agents list')
     data = what['items']
 
     # pre-2022-05 orchestrators where returning a dictionary, not a list
     # of manifests.
     if isinstance(data, dict):
         data = []
         for agent_id, manifest in what['items'].items():
             manifest['metadata']['agent_id'] = agent_id
             data.append(manifest)
-    generate_output(data, AGENT_COLUMNS, AGENT_COLUMNS)
+    return data
 
 
-def _complete_agentid(prefix: str) -> str:
-    response = _get(
-        _agentchannel(),
-        '/agents',
-        'Could not get agents list',
-    )
-    ids = [
-        agent['metadata']['agent_id']
-        for agent in response['items']
-        if agent['metadata']['agent_id'].startswith(prefix)
-    ]
-    if len(ids) == 1:
-        return ids.pop()
-    if ids:
-        _error(
-            'Ambiguous subscription ID prefix "%s", matches:\n- %s',
-            prefix,
-            '\n- '.join(ids),
-        )
-    else:
-        _error('Subscription ID prefix "%s" matches no known subscription ID.', prefix)
-    sys.exit(2)
+def list_agents() -> None:
+    """List all active agents.
+
+    Outputs information in requested format.
+
+    # Raised exceptions
+
+    Abort with an error code 1 if the orchestrator replied with a non-ok
+    code.
+
+    Abort with an error code 2 if another error occurred.
+    """
+    generate_output(_get_agents(), AGENT_COLUMNS, AGENT_COLUMNS)
 
 
 def delete_agent(agent_id: str) -> None:
     """Deregister agent."""
-    agent_id = _ensure_uuid(agent_id, complete=_complete_agentid)
+    agent_id = _ensure_uuid(
+        agent_id, lambda: [agent['metadata']['agent_id'] for agent in _get_agents()]
+    )
     what = _delete(
         _agentchannel(), f'/agents/{agent_id}', f'Could not delete agent {agent_id}'
     )
     print(what['message'])
 
 
 ## JWT tokens
```

## opentf/tools/ctlcommons.py

```diff
@@ -109,37 +109,30 @@
             if what == option:
                 processed[index] = None
                 return option
         return False
 
     max_index = len(args) - 1
     processed: List[Optional[str]] = list(args)
-    options = []
-    allowed = []
-    multis = []
-    for option in COMMON_OPTIONS + list(extra):
-        options.append(tuple(f'{alias.rstrip("=")}=' for alias in option))
-        for alias in options[-1]:
-            allowed.append(alias)
+    options = [
+        tuple(f'{alias.rstrip("=")}=' for alias in option)
+        for option in COMMON_OPTIONS + list(extra)
+    ]
     for option in flags:
         options.append(option)
-        for alias in option:
-            allowed.append(alias)
-    for option in multi:
-        for alias in option:
-            multis.append(f'{alias.rstrip("=")}=')
+    allowed = [alias for option in options for alias in option]
+    multis = [f'{alias.rstrip("=")}=' for option in multi for alias in option]
 
     for index, item in enumerate(args):
         if processed[index] is None:
             continue
         if found := _processed(item.replace('_', '-'), allowed):
             _consume(found)
             continue
-        if _processed(item.replace('_', '-'), multis):
-            continue
+        _processed(item.replace('_', '-'), multis)
 
     return [arg for arg in processed if arg is not None]
 
 
 def _ensure_options(
     command: str, args: List[str], extra=(), multi=(), flags=()
 ) -> Optional[str]:
@@ -419,30 +412,42 @@
 
     # Required parameters
 
     - parameter: a string (an UUID or the begining of one)
 
     # Optional parameters
 
-    - complete: a function of one parameter or None (None by default)
+    - complete: a function of no parameter or None (None by default)
 
     # Raised exceptions
 
     Abort with error code 2 if `parameter` is not a valid UUID.
     """
     if len(parameter) < len(UUID_TEMPLATE):
         if not re.match(UUID_REGEX, parameter + UUID_TEMPLATE[len(parameter) :]):
             _error(
                 'Parameter %s is not a valid UUID.  UUIDs should only contains '
                 'digits, dashes ("-"), and lower case letters ranging from "a" to "f".',
                 parameter,
             )
             sys.exit(2)
         if complete is not None:
-            parameter = complete(parameter)
+            ids = [item for item in complete() if item.startswith(parameter)]
+            if len(ids) == 1:
+                parameter = ids[0]
+            elif len(ids) > 1:
+                _error(
+                    'Ambiguous ID prefix "%s", matches:\n- %s',
+                    parameter,
+                    '\n- '.join(ids),
+                )
+                sys.exit(2)
+            else:
+                _error('ID prefix "%s" matches no known ID.', parameter)
+                sys.exit(2)
     if not re.match(UUID_REGEX, parameter):
         _error(
             'Parameter %s is not a valid UUID.  UUIDs should only contains '
             'digits, dashes ("-"), and lower case letters ranging from "a" to "f", '
             'and are 36 characters long.',
             parameter,
         )
```

## opentf/tools/ctlqualitygate.py

```diff
@@ -23,15 +23,15 @@
     _is_command,
     _get_arg,
     generate_output,
     _ensure_uuid,
     _error,
     _warning,
 )
-from opentf.tools.ctlworkflows import _file_not_found, _complete_workflowid
+from opentf.tools.ctlworkflows import _file_not_found, _get_workflows
 from opentf.tools.ctlconfig import read_configuration
 from opentf.tools.ctlnetworking import (
     _qualitygate,
     _get_json,
     _post,
 )
 
@@ -219,15 +219,15 @@
     except Exception as err:
         _fatal(f'Exception while handling quality gate definition file: {err}')
 
 
 def _query_qualitygate(
     workflow_id: str, using: Optional[str], mode: str
 ) -> Dict[str, Any]:
-    workflow_id = _ensure_uuid(workflow_id, _complete_workflowid)
+    workflow_id = _ensure_uuid(workflow_id, _get_workflows)
     if using:
         result = _post_qualitygate_definition(workflow_id, using, mode)
     else:
         result = _get_qualitygate_status(workflow_id, mode)
     return _process_qualitygate_result(result, workflow_id)
```

## opentf/tools/ctlworkflows.py

```diff
@@ -308,24 +308,27 @@
         _error(
             'Could not get workflows list.  Return code was %d.',
             response.status_code,
         )
     sys.exit(2)
 
 
-def list_workflows() -> None:
-    """List active and recent workflows."""
+def _get_workflows() -> List[str]:
     response = _get_json(
         _observer(),
         '/workflows',
         'Could not get workflows list',
         handler=_handle_maybe_outdated,
     )
-    workflows_ids = response['details']['items']
-    generate_output(list(_get_manifests(workflows_ids)), DEFAULT_COLUMNS, WIDE_COLUMNS)
+    return response['details']['items']
+
+
+def list_workflows() -> None:
+    """List active and recent workflows."""
+    generate_output(_get_manifests(_get_workflows()), DEFAULT_COLUMNS, WIDE_COLUMNS)
 
 
 def _handle_maybe_details(response) -> NoReturn:
     _error(response.json()['message'])
     if response.json().get('details'):
         _error(response.json()['details'].get('error'))
     sys.exit(1)
@@ -646,35 +649,14 @@
             if 'next' in response.links:
                 break
             if current_item == 0 and len(status['details']['items']) == 0:
                 _warning(f'Could not find items matching selectors: {params}')
                 break
 
 
-def _complete_workflowid(prefix: str) -> str:
-    response = _get_json(
-        _observer(),
-        '/workflows',
-        'Could not get workflows list',
-        handler=_handle_maybe_outdated,
-    )
-    ids = [ids for ids in response['details']['items'] if ids.startswith(prefix)]
-    if len(ids) == 1:
-        return ids.pop()
-    if ids:
-        _error(
-            'Ambiguous workflow ID prefix "%s", matches:\n- %s',
-            prefix,
-            '\n- '.join(ids),
-        )
-    else:
-        _error('Workflow ID prefix "%s" matches no known workflow ID.', prefix)
-    sys.exit(2)
-
-
 def get_workflow(workflow_id: str, watch=False) -> None:
     """Get a workflow.
 
     # Required parameters
 
     - workflow_id: a string
 
@@ -689,15 +671,15 @@
     # Raised exceptions
 
     Abort with an error code 1 if the workflow could not be found on the
     orchestrator.
 
     Abort with an error code 2 if another error occurred.
     """
-    workflow_id = _ensure_uuid(workflow_id, _complete_workflowid)
+    workflow_id = _ensure_uuid(workflow_id, _get_workflows)
 
     cancelation_event = None
     job_depth = _get_arg('--job-depth=') or _get_arg('-j=') or CONFIG.get('job-depth')
     if job_depth is None:
         job_depth = 1
     try:
         job_depth = int(job_depth)
@@ -810,15 +792,15 @@
         if response.status_code == 404:
             _error(f'Workflow {workflow_id} is not known.')
         else:
             _error(f'Could not check if workflow {workflow_id} exists.')
         _error('Could not kill workflow.')
         sys.exit(1)
 
-    workflow_id = _ensure_uuid(workflow_id, _complete_workflowid)
+    workflow_id = _ensure_uuid(workflow_id, _get_workflows)
 
     _ = _get(_observer(), f'/workflows/{workflow_id}/status', handler=_notknown)
     _ = _delete(_killswitch(), f'/workflows/{workflow_id}')
     print(f'Killing workflow {workflow_id}.')
 
 
 ########################################################################
```

## Comparing `opentf_tools_nightly-0.43.0.dev486.dist-info/LICENSE` & `opentf_tools_nightly-0.43.0.dev491.dist-info/LICENSE`

 * *Files identical despite different names*

## Comparing `opentf_tools_nightly-0.43.0.dev486.dist-info/METADATA` & `opentf_tools_nightly-0.43.0.dev491.dist-info/METADATA`

 * *Files 0% similar despite different names*

```diff
@@ -1,10 +1,10 @@
 Metadata-Version: 2.1
 Name: opentf-tools-nightly
-Version: 0.43.0.dev486
+Version: 0.43.0.dev491
 Summary: OpenTestFactory Orchestrator Tools
 Home-page: https://opentestfactory.org/tools/
 Author: Martin Lafaix
 Author-email: mlafaix@henix.com
 Maintainer: Henix
 Maintainer-email: opentestfactory@henix.com
 License: Apache Software License (https://www.apache.org/licenses/LICENSE-2.0)
```

## Comparing `opentf_tools_nightly-0.43.0.dev486.dist-info/RECORD` & `opentf_tools_nightly-0.43.0.dev491.dist-info/RECORD`

 * *Files 17% similar despite different names*

```diff
@@ -1,14 +1,14 @@
-opentf/tools/ctl.py,sha256=XJ3qv0pwfUrMb5qpBHlC_y_BDJtZ_rkt4YCBYCXC9IY,25131
-opentf/tools/ctlcommons.py,sha256=qKPLrj77C9Eff0e6U82xB6GwONIcumlnol2H_ARbHkw,14035
+opentf/tools/ctl.py,sha256=iuy20BAHxFIuexWOrw7R9JHMXzeT1_N2jmsA0HB_ceA,24013
+opentf/tools/ctlcommons.py,sha256=OCq5VBoeb0FNPXd5KxAQE-JXGkNyFmK-UaHtIVeCjrM,14327
 opentf/tools/ctlconfig.py,sha256=zFZ2YgJP0W1Vr4u6U6BJX_PVrU00aLkLgLYQoyyykxc,32425
 opentf/tools/ctlnetworking.py,sha256=NO_U5VZ49w5vEcbOzX7cjXFY8ygsOb6NaoEgj4U1oW0,8878
-opentf/tools/ctlqualitygate.py,sha256=ibDBUW-hc3AaynOFHyBj1autQPhsH1JTOv8MPjXyLfc,11908
-opentf/tools/ctlworkflows.py,sha256=Vmnzhow6Fo69TAFWgKEJpe3z8eMVWT9P-Q_tEKK-Wkg,29105
+opentf/tools/ctlqualitygate.py,sha256=rYZtAO0uP-T6KptgASj67a9jvRz7WtCyEqdnykM9SL4,11896
+opentf/tools/ctlworkflows.py,sha256=HWs7Wg8t03x-l-52wBKne-Y6ZpwTZNi5WRPT7Aka-Ew,28527
 opentf/tools/done.py,sha256=1X5s9IjpphD0LZvcQ0h36mKHkbLjjYDzlqqwat6kKdE,3188
 opentf/tools/ready.py,sha256=WHPdnWCMOKLCxLlF0FTCCpW-8y0BF7rrGCl7H2AJ-DM,9391
-opentf_tools_nightly-0.43.0.dev486.dist-info/LICENSE,sha256=xx0jnfkXJvxRnG63LTGOxlggYnIysveWIZ6H3PNdCrQ,11357
-opentf_tools_nightly-0.43.0.dev486.dist-info/METADATA,sha256=780cxhSR6fRR_Vm6QQqKK9hILJ5TwnSRjEUiSklV8UM,1806
-opentf_tools_nightly-0.43.0.dev486.dist-info/WHEEL,sha256=AtBG6SXL3KF_v0NxLf0ehyVOh0cold-JbJYXNGorC6Q,92
-opentf_tools_nightly-0.43.0.dev486.dist-info/entry_points.txt,sha256=u5qYtmUbgIMcq8360YobnI9CtDvrjptf4lDkmKojoa0,139
-opentf_tools_nightly-0.43.0.dev486.dist-info/top_level.txt,sha256=_gPuE6GTT6UNXy1DjtmQSfCcZb_qYA2vWmjg7a30AGk,7
-opentf_tools_nightly-0.43.0.dev486.dist-info/RECORD,,
+opentf_tools_nightly-0.43.0.dev491.dist-info/LICENSE,sha256=xx0jnfkXJvxRnG63LTGOxlggYnIysveWIZ6H3PNdCrQ,11357
+opentf_tools_nightly-0.43.0.dev491.dist-info/METADATA,sha256=N2UIvFMBfZrZQfJjcCvjMk9muQDi8myuxKi3pftdZZ8,1806
+opentf_tools_nightly-0.43.0.dev491.dist-info/WHEEL,sha256=AtBG6SXL3KF_v0NxLf0ehyVOh0cold-JbJYXNGorC6Q,92
+opentf_tools_nightly-0.43.0.dev491.dist-info/entry_points.txt,sha256=u5qYtmUbgIMcq8360YobnI9CtDvrjptf4lDkmKojoa0,139
+opentf_tools_nightly-0.43.0.dev491.dist-info/top_level.txt,sha256=_gPuE6GTT6UNXy1DjtmQSfCcZb_qYA2vWmjg7a30AGk,7
+opentf_tools_nightly-0.43.0.dev491.dist-info/RECORD,,
```

